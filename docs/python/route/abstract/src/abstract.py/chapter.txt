! CONTROL:
	/type/
		module
	/element/
		- (control)`property-set`
		- (source/area)`1 1 681 0`
		- (coverage-zeros)`118`
		- (coverage-counters)`177`
		- (metrics/profile-duration)`287000`
		- (metrics/profile-count)`10`
		- (metrics/profile-residency)`100.0`
		- (metrics/profile-minimum)`287000`
		- (metrics/profile-maximum)`287000`
Route interface descriptions for (factor-local/class)&<#Path[Path]> manipulations and (factor-local/class)&<#File[File]> system controls.

[ File System Property Codes ]

(factor-local/class)&<#File[File]> operations that analyze status properties use character codes to
perform filtering. The codes listed here are primarily for POSIX file systems
and may have extensions or different meanings when used with emulated file systems.

[ > Permissions ]

Character codes identifying available permissions.

/`'r'`/
	Readable.

/`'w'`/
	Writable.

/`'x'`/
	Executable or searchable.

[ > Types ]

Character codes identifying a type of file.

/`'*'`/
	Any.

/`'!'`/
	Void; file must not exist.

/`'/'`/
	Directory.

/`'.'`/
	Data. A "regular" file.

/`'&'`/
	A symbolic link.

/`'|'`/
	A named pipe.

/`'@'`/
	A file system socket file.

/`'#'`/
	A device file.

/`'?'`/
	Unknown type.
	File exists, but it's type is not known and may be inaccessible.


[ abstractmethod ]
! CONTROL:
	/type/
		import
	/flags/
		- `undocumented`
	/element/
		- (control)`property-set`
		- (source/area)`43 1 43 30`

[ Hashable ]
! CONTROL:
	/type/
		import
	/flags/
		- `undocumented`
	/element/
		- (control)`property-set`
		- (source/area)`44 1 44 65`

[ Iterable ]
! CONTROL:
	/type/
		import
	/flags/
		- `undocumented`
	/element/
		- (control)`property-set`
		- (source/area)`44 1 44 65`

[ Sequence ]
! CONTROL:
	/type/
		import
	/flags/
		- `undocumented`
	/element/
		- (control)`property-set`
		- (source/area)`44 1 44 65`

[ Mapping ]
! CONTROL:
	/type/
		import
	/flags/
		- `undocumented`
	/element/
		- (control)`property-set`
		- (source/area)`44 1 44 65`
		- (coverage-zeros)`0`
		- (coverage-counters)`1`

[ Protocol ]
! CONTROL:
	/type/
		import
	/flags/
		- `undocumented`
	/element/
		- (control)`property-set`
		- (source/area)`45 1 45 48`

[ TypeAlias ]
! CONTROL:
	/type/
		import
	/flags/
		- `undocumented`
	/element/
		- (control)`property-set`
		- (source/area)`45 1 45 48`

[ Type ]
! CONTROL:
	/type/
		import
	/flags/
		- `undocumented`
	/element/
		- (control)`property-set`
		- (source/area)`45 1 45 48`

[ IO ]
! CONTROL:
	/type/
		import
	/flags/
		- `undocumented`
	/element/
		- (control)`property-set`
		- (source/area)`45 1 45 48`
		- (coverage-zeros)`0`
		- (coverage-counters)`1`

[ AbstractContextManager ]
! CONTROL:
	/type/
		import
	/flags/
		- `undocumented`
	/element/
		- (control)`property-set`
		- (source/area)`46 1 46 45`
		- (coverage-zeros)`0`
		- (coverage-counters)`1`

[ Element ]
! CONTROL:
	/type/
		data
	/flags/
		- `undocumented`
	/element/
		- (control)`property-set`
		- (source/area)`48 1 48 61`
		- (coverage-zeros)`0`
		- (coverage-counters)`1`
		- (type/syntax)`TypeAlias`
		- (type/none/invalid)&<http://fault.io/dev/null[typing.TypeAlias]>
		
#!source
	Element: TypeAlias = tuple[str, Sequence['Element'], Mapping]

[ sys ]
! CONTROL:
	/type/
		import
	/flags/
		- `undocumented`
	/element/
		- (control)`property-set`
		- (source/area)`51 1 51 10`
		- (coverage-zeros)`0`
		- (coverage-counters)`1`

[ Path ]
! CONTROL:
	/type/
		class
	/element/
		- (control)`property-set`
		- (source/area)`59 1 211 27`
		- (coverage-zeros)`0`
		- (coverage-counters)`1`
! INHERIT:
	- (control)`property-set`
	- (source/area)`59 12 59 19`
	- (type/syntax)`Protocol`
	- (type/none/invalid)&<http://fault.io/dev/null[typing.Protocol]>
	
Primitive operations for manipulating a sequence of identifiers.


[ Path >> container ]
! CONTROL:
	/type/
		property
	/element/
		- (control)`property-set`
		- (source/area)`66 2 75 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`Path`
		- (type/factor-local/class)&<#Path[Path]>
		
The route containing the resource identified by (invalid/unknown)&<#self>.

For file systems, this is the parent directory.


[ Path >> absolute ]
! CONTROL:
	/type/
		property
	/element/
		- (control)`property-set`
		- (source/area)`76 2 83 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`list[Hashable]`
		
The absolute sequence of identifiers that make up the path.


[ Path >> identifier ]
! CONTROL:
	/type/
		property
	/element/
		- (control)`property-set`
		- (source/area)`84 2 91 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`Hashable`
		- (type/none/invalid)&<http://fault.io/dev/null[collections.abc.Hashable]>
		
The identification of the resource relative to its immediate (factor-local/property)&<#Path.container[container]>.
The last point in the route.


[ Path >> prefix ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`92 2 98 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`2`
(signature)`prefix(self, string)`

Construct a new route with the given (factor-local/abstractmethod)&<#Path.prefix[prefix]> added to the beginning of the identifier.


[ Path >> suffix ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`99 2 105 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`2`
(signature)`suffix(self, string)`

Construct a new route with the given (factor-local/abstractmethod)&<#Path.suffix[suffix]> added to the end of the identifier.


[ Path >> truncate ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`106 2 114 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`Path`
		- (type/factor-local/class)&<#Path[Path]>
		
(signature)`truncate(self, identifier)`

Construct a new route consisting of the existing sequence of points up to the *last*
point specified by the (invalid/unknown)&<#point> argument. Similar to slicing a sequence from a
reverse index search.


[ Path >> __invert__ ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`115 2 124 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`Iterable[Path]`
		
(signature)`__invert__(self)`

Stepwise root ascension.

#!python
	assert list(~route) == [route ** 1, route ** 2, ..., route ** len(route)]

[ Path >> __lshift__ ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`125 2 131 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`Iterable[Path]`
		
(signature)`__lshift__(self, route)`

Stepwise ascension path.


[ Path >> __rshift__ ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`132 2 138 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`Iterable[Path]`
		
(signature)`__rshift__(self, route)`

Stepwise descension path.


[ Path >> __xor__ ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`139 2 145 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`Iterable[Path]`
		
(signature)`__xor__(self, route)`

Stepwise traverse path.


[ Path >> __matmul__ ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`146 2 157 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`Path`
		- (type/factor-local/class)&<#Path[Path]>
		
(signature)`__matmul__(self, path_expression)`

Composite extension.
Construct a new route by extending (factor-local/parameter)&<#Path.__matmul__.Parameters.self[self]> with the points expressed in (factor-local/parameter)&<#Path.__matmul__.Parameters.path_expression[path_expression]>.

[ >> Parameters ]

/(parameter)`self`/
	
	(control/absent)`Undocumented`.

/(parameter)`path_expression`/
	- (type/syntax)`str`
	- (type/invalid/unknown)&<#str>
	
	A string that represents a relative or absolute path.


[ Path >> __add__ ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`158 2 168 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`Path`
		- (type/factor-local/class)&<#Path[Path]>
		
(signature)`__add__(self, points)`

Extension by iterable.
Construct a new route by extending (factor-local/parameter)&<#Path.__add__.Parameters.self[self]> with (factor-local/parameter)&<#Path.__add__.Parameters.points[points]>.

#!python
	assert (route + points) == (route / points[0] / points[1] ... / points[n])

[ Path >> __floordiv__ ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`169 2 179 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`Path`
		- (type/factor-local/class)&<#Path[Path]>
		
(signature)`__floordiv__(self, route)`

Segment extension.
Construct a new route by extending (factor-local/parameter)&<#Path.__floordiv__.Parameters.self[self]> with all the points in (factor-local/parameter)&<#Path.__floordiv__.Parameters.route[route]>.

#!python
	assert (route // segment) == (route + segment.absolute)

[ Path >> __truediv__ ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`180 2 190 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`Path`
		- (type/factor-local/class)&<#Path[Path]>
		
(signature)`__truediv__(self, point)`

Single extension.
Construct a new route by extending (factor-local/parameter)&<#Path.__truediv__.Parameters.self[self]> with a single (factor-local/parameter)&<#Path.__truediv__.Parameters.point[point]>.

#!python
	assert (route / identifier) == (route + [identifier])

[ Path >> __mul__ ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`191 2 201 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`Path`
		- (type/factor-local/class)&<#Path[Path]>
		
(signature)`__mul__(self, identifier)`

Identifier substitution.
Construct a new route by extending (factor-local/parameter)&<#Path.__mul__.Parameters.self[self.container]> with (factor-local/parameter)&<#Path.__mul__.Parameters.identifier[identifier]>.

#!python
	assert (route * 'replacement') == (route.container / 'replacement')

[ Path >> __pow__ ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`202 2 211 27`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`Path`
		- (type/factor-local/class)&<#Path[Path]>
		
(signature)`__pow__(self, nth)`

Numeric ascension operation.
Construct a new route representing the (factor-local/parameter)&<#Path.__pow__.Parameters.nth[nth]> container of (factor-local/parameter)&<#Path.__pow__.Parameters.self[self]>.

#!python
	assert (route ** 1) == (route.container)
	assert (route ** 2) == (route.container.container)

[ Status ]
! CONTROL:
	/type/
		class
	/element/
		- (control)`property-set`
		- (source/area)`213 1 329 27`
		- (coverage-zeros)`0`
		- (coverage-counters)`1`
! INHERIT:
	- (control)`property-set`
	- (source/area)`213 14 213 21`
	- (type/syntax)`Protocol`
	- (type/none/invalid)&<http://fault.io/dev/null[typing.Protocol]>
	
Data class holding common file status fields.


[ Status >> unit ]
! CONTROL:
	/type/
		property
	/element/
		- (control)`property-set`
		- (source/area)`220 2 230 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`str`
		- (type/invalid/unknown)&<#str>
		
The unit that (factor-local/property)&<#Status.size[size]> is measured with.

Presumed to be `'bytes'` for file systems, but may not be a concrete
measure of information storage for some systems.


[ Status >> system ]
! CONTROL:
	/type/
		property
	/element/
		- (control)`property-set`
		- (source/area)`231 2 239 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`object`
		- (type/invalid/unknown)&<#object>
		
System specific data structure holding the status data.
Usually (invalid/unknown)&<#os.stat_result>.


[ Status >> size ]
! CONTROL:
	/type/
		property
	/element/
		- (control)`property-set`
		- (source/area)`240 2 250 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`int`
		- (type/invalid/unknown)&<#int>
		
Count of (factor-local/property)&<#Status.unit[unit]> contained by the file.

#!syntax/python
	assert (route.fs_size() == route.fs_status().size)

[ Status >> type ]
! CONTROL:
	/type/
		property
	/element/
		- (control)`property-set`
		- (source/area)`251 2 273 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`str`
		- (type/invalid/unknown)&<#str>
		
String symbol identifying the type of file.

/`'void'`/
	A broken link or nonexistent file.

/`'directory'`/
	A file containing other files.

/`'data'`/
	A regular file containing bytes.

/`'link'`/
	Status record of a link to a file.

/`'pipe'`/
	A named pipe; also known as a FIFO. (POSIX)

/`'socket'`/
	A unix domain socket. (POSIX)

/`'device'`/
	A character or block device file. (POSIX)


[ Status >> subtype ]
! CONTROL:
	/type/
		property
	/element/
		- (control)`property-set`
		- (source/area)`274 2 284 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`str|None:`
		
For POSIX-type systems, designates the kind of (id)`device`:
(id)`block` or (id)`character`.

(invalid/unknown)&<#None> for status instances whose (factor-local/property)&<#Status.type[type]> is not (id)`device`.


[ Status >> executable ]
! CONTROL:
	/type/
		property
	/element/
		- (control)`property-set`
		- (source/area)`285 2 292 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`bool`
		- (type/invalid/unknown)&<#bool>
		
Whether the data file is considered executable by anyone.


[ Status >> searchable ]
! CONTROL:
	/type/
		property
	/element/
		- (control)`property-set`
		- (source/area)`293 2 300 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`bool`
		- (type/invalid/unknown)&<#bool>
		
Whether the directory file is considered searchable by anyone.


[ Status >> created ]
! CONTROL:
	/type/
		property
	/element/
		- (control)`property-set`
		- (source/area)`301 2 308 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`2`
Time of creation; UTC. Not available on all systems.


[ Status >> last_modified ]
! CONTROL:
	/type/
		property
	/element/
		- (control)`property-set`
		- (source/area)`309 2 316 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`2`
Time of last modification; UTC.


[ Status >> last_accessed ]
! CONTROL:
	/type/
		property
	/element/
		- (control)`property-set`
		- (source/area)`317 2 324 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`2`
Time of last access; UTC.


[ Status >> meta_last_modified ]
! CONTROL:
	/type/
		property
	/element/
		- (control)`property-set`
		- (source/area)`325 2 329 27`
		- (coverage-zeros)`2`
		- (coverage-counters)`2`
Time of last status change; UTC.


[ File ]
! CONTROL:
	/type/
		class
	/element/
		- (control)`property-set`
		- (source/area)`331 1 681 27`
		- (coverage-zeros)`0`
		- (coverage-counters)`1`
! INHERIT:
	- (control)`property-set`
	- (source/area)`331 12 331 15`
	- (type/syntax)`Path`
	- (type/factor-local/class)&<#Path[Path]>
	
File system APIs for supporting common access functions.


[ File >> Status ]
! CONTROL:
	/type/
		property
	/element/
		- (control)`property-set`
		- (source/area)`338 2 345 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`Type[FileStatus]`
		
The status data structure used by the Path.


[ File >> Violation ]
! CONTROL:
	/type/
		property
	/element/
		- (control)`property-set`
		- (source/area)`346 2 354 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`Type[Exception]`
		
Exception describing the property violations found
by a call to (factor-local/abstractmethod)&<#File.fs_require[fs_require]>.


[ File >> filename ]
! CONTROL:
	/type/
		property
	/element/
		- (control)`property-set`
		- (source/area)`355 2 362 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`str`
		- (type/invalid/unknown)&<#str>
		
File system specific alias for (invalid/unknown)&<#identifier>.


[ File >> extension ]
! CONTROL:
	/type/
		property
	/element/
		- (control)`property-set`
		- (source/area)`363 2 371 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`str|None:`
		
The last dot-extension of the filename.

(invalid/unknown)&<#None> if the filename has no `.` characters at all.


[ File >> __pos__ ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`372 2 378 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`File`
		- (type/factor-local/class)&<#File[File]>
		
(signature)`__pos__(self)`

Resolve any relative path components, `.` and `..`, in (factor-local/parameter)&<#File.__pos__.Parameters.self[self]>.


[ File >> fs_path_string ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`379 2 391 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`str`
		- (type/invalid/unknown)&<#str>
		
(signature)`fs_path_string(self)`

Construct a normalized string representing the path to the file.

Relative resolution must still be explicitly performed, but empty
path entries delimiting partitions are eliminated.

(factor-local/class)&<#File[File]> protocol name suitable for use as `__fspath__` implementation.


[ File >> fs_tmpdir ]
! CONTROL:
	/type/
		classmethod
	/element/
		- (control)`property-set`
		- (source/area)`392 2 400 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`AbstractContextManager[File]`
		
(signature)`fs_tmpdir(Class)`

Constuct a context manager that creates a temporary directory when entered.

On exit, the directory and its contents are destroyed.


[ File >> fs_open ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`401 2 412 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`AbstractContextManager[IO]`
		
(signature)`fs_open(self, mode)`

Open the file identified by the path, (factor-local/parameter)&<#File.fs_open.Parameters.self[self]>.

The file is opened when the returned context manager is entered and closed on exit.

The parameters match Python's (invalid/unknown)&<#open> builtin without `opener` and `closefd` where
the behavior is always the default.


[ File >> fs_require ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`413 2 434 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`2`
(signature)`fs_require(self, properties, type)`

Check the file for the expressed requirements.
The (factor-local/parameter)&<#File.fs_require.Parameters.properties[properties]> string consists of characters described by
&[File System Property Codes].

[ >> Parameters ]

/(parameter)`self`/
	
	(control/absent)`Undocumented`.

/(parameter)`properties`/
	- (type/syntax)`str`
	- (type/invalid/unknown)&<#str>
	
	The required type, permissions and option control flags.

/(parameter)`type`/
	
	The required file type, inclusive.
	When (invalid/unknown)&<#None>, the default, the file type must not be a directory.

	Overrides any file type codes present in (factor-local/parameter)&<#File.fs_require.Parameters.properties[properties]>.

[ >> Exceptions ]

/(factor-local/property)&<#File.Violation[Violation]>/
	Raised when a designated property is not present on the file.


[ File >> fs_replace ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`435 2 447 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`2`
(signature)`fs_replace(self, replacement)`

Destroy the existing file or directory, (factor-local/parameter)&<#File.fs_replace.Parameters.self[self]>, and replace it with the
file or directory at the given route, (factor-local/parameter)&<#File.fs_replace.Parameters.replacement[replacement]>.

[ >> Parameters ]

/(parameter)`self`/
	
	(control/absent)`Undocumented`.

/(parameter)`replacement`/
	
	The route to the file or directory that will be used to replace
	the one at (factor-local/parameter)&<#File.fs_replace.Parameters.self[self]>.


[ File >> fs_void ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`448 2 455 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`2`
(signature)`fs_void(self)`

Destroy the file or directory at the location identified by (factor-local/parameter)&<#File.fs_void.Parameters.self[self]>.
For directories, this recursively removes content as well.


[ File >> fs_reduce ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`456 2 463 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`2`
(signature)`fs_reduce(self, discarded)`

Relocate the directory contents in (factor-local/parameter)&<#File.fs_reduce.Parameters.discarded[discarded]> into (factor-local/parameter)&<#File.fs_reduce.Parameters.self[self]>, and
destroy the segment of directories between (factor-local/parameter)&<#File.fs_reduce.Parameters.self[self]> and (factor-local/parameter)&<#File.fs_reduce.Parameters.discarded[discarded]>.


[ File >> fs_alloc ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`464 2 472 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`2`
(signature)`fs_alloc(self)`

Allocate the necessary resources to create the target path as a file or directory.

Normally, this means creating the *leading* path to the identified resource.


[ File >> fs_follow_links ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`473 2 482 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`Iterable[File]`
		
(signature)`fs_follow_links(self)`

Follow the symbolic links back to the final non-link file.

The followed paths are not further resolved. Any links leading
to links or the final file will remain in the yielded paths.


[ File >> fs_link_relative ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`483 2 494 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`2`
(signature)`fs_link_relative(self, path)`

Create or update a *symbolic* link at (factor-local/parameter)&<#File.fs_link_relative.Parameters.self[self]> pointing to (factor-local/parameter)&<#File.fs_link_relative.Parameters.path[path]>, the target file.
The linked target path will be relative to (invalid/unknown)&<#self'> route.

[ >> Parameters ]

/(parameter)`self`/
	
	(control/absent)`Undocumented`.

/(parameter)`path`/
	
	The route identifying the target path of the symbolic link.


[ File >> fs_link_absolute ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`495 2 506 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`2`
(signature)`fs_link_absolute(self, path)`

Create or update a *symbolic* link at (factor-local/parameter)&<#File.fs_link_absolute.Parameters.self[self]> pointing to (factor-local/parameter)&<#File.fs_link_absolute.Parameters.path[path]>, the target file.
The linked target path will be absolute.

[ >> Parameters ]

/(parameter)`self`/
	
	(control/absent)`Undocumented`.

/(parameter)`path`/
	
	The route identifying the target path of the symbolic link.


[ File >> fs_mkdir ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`507 2 513 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`2`
(signature)`fs_mkdir(self)`

Create a directory at the location referenced by (factor-local/parameter)&<#File.fs_mkdir.Parameters.self[self]>.


[ File >> fs_real ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`514 2 520 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`File`
		- (type/factor-local/class)&<#File[File]>
		
(signature)`fs_real(self)`

Identify the portion of the route that actually exists on the file system.


[ File >> fs_linear ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`521 2 533 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`File`
		- (type/factor-local/class)&<#File[File]>
		
(signature)`fs_linear(self)`

Identify the next non-linear directory.

Recursively scan the file system until a directory is found containing
zero files, more than one file, or a sole non-directory file is found.

[ >> Returns ]

The path to the next non-linear directory as a (factor-local/class)&<#File[File]>.


[ File >> fs_status ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`534 2 540 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`Status`
		- (type/factor-local/property)&<#File.Status[Status]>
		
(signature)`fs_status(self)`

Construct a data structure representing the latest status of the file.


[ File >> fs_load ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`541 2 547 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`bytes`
		- (type/invalid/unknown)&<#bytes>
		
(signature)`fs_load(self)`

Retrieve the binary data stored at the location identified by (factor-local/parameter)&<#File.fs_load.Parameters.self[self]>.


[ File >> fs_store ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`548 2 554 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`2`
(signature)`fs_store(self, data)`

Store the given (factor-local/parameter)&<#File.fs_store.Parameters.data[data]> at the location referenced by (factor-local/parameter)&<#File.fs_store.Parameters.self[self]>.


[ File >> fs_type ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`555 2 573 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`str`
		- (type/invalid/unknown)&<#str>
		
(signature)`fs_type(self)`

A string identifying the type of file selected by the (invalid/unknown)&<#Route>.
Often a shorthand for accessing the type from the structure
returned by (factor-local/abstractmethod)&<#File.fs_status[fs_status]>.

[ >> Returns ]

- `'directory'`
- `'data'`
- `'pipe'`
- `'socket'`
- `'device'`
- `'void'`

If no file is present at the path or a broken link is present, `'void'` will be returned.


[ File >> fs_size ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`574 2 582 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`int`
		- (type/invalid/unknown)&<#int>
		
(signature)`fs_size(self)`

Return the size, count of bytes, of the file's content.

- `file.fs_size() == file.fs_status().size`


[ File >> fs_executable ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`583 2 591 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`bool`
		- (type/invalid/unknown)&<#bool>
		
(signature)`fs_executable(self)`

Whether or not the regular file is executable.

Directories marked as executable are not considered executables.


[ File >> fs_searchable ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`592 2 600 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`bool`
		- (type/invalid/unknown)&<#bool>
		
(signature)`fs_searchable(self)`

Whether or not the directory's listing can be retrieved.

Regular files marked as executable are not considered searchable.


[ File >> fs_since ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`601 2 614 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`Iterable[File]`
		
(signature)`fs_since(self, time, inverse)`

Select data files within the path, (factor-local/parameter)&<#File.fs_since.Parameters.self[self]>, whose modification time exceeds (factor-local/parameter)&<#File.fs_since.Parameters.time[time]>.

[ >> Parameters ]

/(parameter)`self`/
	
	(control/absent)`Undocumented`.

/(parameter)`time`/
	- (type/syntax)`object|None=`
	
	The referennce time. When unspecified as (invalid/unknown)&<#None>, the modification time
	of the directory identified by (factor-local/parameter)&<#File.fs_since.Parameters.self[self]> is used.

/(parameter)`inverse`/
	- (type/syntax)`bool`
	- (type/invalid/unknown)&<#bool>
	
	Select files modified before (factor-local/parameter)&<#File.fs_since.Parameters.time[time]> when (invalid/unknown)&<#True>.


[ File >> fs_modified ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`615 2 624 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`2`
(signature)`fs_modified(self)`

Update the modification time of the file identified by (factor-local/parameter)&<#File.fs_modified.Parameters.self[self]>.

The new modification time should reflect the real clock's status on the system
managing the file.


[ File >> fs_snapshot ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`625 2 653 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`Sequence[Element]`
		
(signature)`fs_snapshot(self)`

Construct an element tree of files from the directory identified by (factor-local/parameter)&<#File.fs_snapshot.Parameters.self[self]>.

Exceptions raised by operations populating the tree are trapped
as `'exception'` elements that are filtered by (invalid/unknown)&<#process> by default.

[ >> Parameters ]

/(parameter)`self`/
	
	(control/absent)`Undocumented`.

[ >> Returns ]

The sequence of elements that represent the directory's listing
according to the given arguments.


[ File >> fs_iterfiles ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`654 2 664 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`Iterable[File]`
		
(signature)`fs_iterfiles()`

Generate (factor-local/class)&<#File[File]> instances identifying the files held by the directory, (invalid/unknown)&<#self>.
By default, all file types are included, but if the (factor-local/parameter)&<#File.fs_iterfiles.Parameters.type[type]> parameter is given,
only files of that type are returned.

If (invalid/unknown)&<#self> is not a directory or cannot be searched, an empty iterator is returned.


[ File >> fs_list ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`665 2 674 16`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`tuple[list[File], list[File]]`
		
(signature)`fs_list(self)`

Retrieve the list of files contained by the directory referred to by (factor-local/parameter)&<#File.fs_list.Parameters.self[self]>.
Returns a pair, the sequence of directories and the sequence of data files.

Sockets, pipes, devices, and other non-data files are not retained in the list.


[ File >> fs_index ]
! CONTROL:
	/type/
		abstractmethod
	/element/
		- (control)`property-set`
		- (source/area)`675 2 681 27`
		- (coverage-zeros)`2`
		- (coverage-counters)`3`
		- (type/syntax)`Iterable[tuple[File, list[File]]]`
		
(signature)`fs_index(self)`

Generate pairs of directories associated with their files.

Sockets, pipes, devices, broken links, and other non-data files are not retained in the lists.

